apiVersion: v1
kind: ConfigMap
metadata:
  name: quirk-config
  namespace: quirk
  labels:
    app: quirk
data:
  NODE_ENV: "production"
  PORT: "5000"
  # Update FRONTEND_URL after getting the AKS public IP / domain
  FRONTEND_URL: "http://REPLACE_WITH_YOUR_PUBLIC_IP_OR_DOMAIN"
